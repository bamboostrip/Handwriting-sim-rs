# Tauri 工作区：crates/core 为渲染引擎核心库，src-tauri 为桌面应用壳。
# 前端（Vue 3）位于仓库根目录，符合 create-tauri-app 标准布局。
[workspace]
members = ["crates/core", "src-tauri"]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
