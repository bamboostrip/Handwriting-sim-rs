# 资源目录说明。

- `backgrounds/`、`presets/`：**原创素材**，随仓库分发（发布包也包含），可自由使用与替换。
- `fonts/`：用户自备字体，**不入库**（手写体多为商业版权字体，开源分发需授权）。
  本地测试请把字体放入 exe 旁的 `fonts/` 目录（便携模式以 exe 所在目录为资产根）。
- 测试使用系统字体（Windows `msyh.ttc` / macOS `PingFang.ttc` / Linux Noto CJK），
  找不到时对应测试自动跳过。
